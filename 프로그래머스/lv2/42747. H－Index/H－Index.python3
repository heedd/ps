def solution(citations):
    citations.sort() # 0, 0, 1, 2, 3
    l = len(citations)
    for i in range(len(citations)) :  #1~l-1
        if citations[i] >= l-i :
            return l-i
    return 0
    
 
